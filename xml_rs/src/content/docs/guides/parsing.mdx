---
title: Parsing
description: Parse XML strings, byte vectors and files into an xml_rs document tree.
---

import { Aside } from '@astrojs/starlight/components';

[`XmlDeserializer`](/xml_rs/api-reference/#xmldeserializer) turns XML input into
an [`XmlDocument`](/xml_rs/api-reference/#xmldocument) tree. It reads the XML
declaration, normalizes line breaks, resolves namespaces and preserves comments.

## From a byte vector or string

```rust
use draviavemal_xml_rs::XmlDeserializer;

let xml = r#"<note><to>World</to></note>"#;
let document = XmlDeserializer::vec_to_xml_doc_tree(xml.as_bytes().to_vec())?;
```

`vec_to_xml_doc_tree` takes a `Vec<u8>`, so pass `your_string.as_bytes().to_vec()`
for an in-memory string.

## From a file

```rust
use draviavemal_xml_rs::XmlDeserializer;

let document = XmlDeserializer::file_to_xml_doc_tree("catalog.xml")?;
```

## What is preserved

<table>
  <thead>
    <tr>
      <th>Input</th>
      <th>Handling</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>XML declaration</td>
      <td><code>version</code> and <code>encoding</code> are read onto the document (encoding defaults to <code>utf-8</code>).</td>
    </tr>
    <tr>
      <td>Namespaces</td>
      <td>Prefix → URI mappings are resolved; QNames keep their prefix.</td>
    </tr>
    <tr>
      <td>Attributes</td>
      <td>Kept in source order.</td>
    </tr>
    <tr>
      <td>Text & mixed content</td>
      <td>Unescaped and stored as text nodes.</td>
    </tr>
    <tr>
      <td>Comments</td>
      <td>Preserved as comment nodes and re-emitted on serialize.</td>
    </tr>
    <tr>
      <td>Self-closing tags</td>
      <td>Parsed as empty elements.</td>
    </tr>
  </tbody>
</table>

<Aside type="caution">
Input is treated as UTF-8. A mismatched start/end tag fails parsing with a
descriptive `anyhow` error, so handle the `Result`.
</Aside>

## Next

- [Navigating & querying](/xml_rs/guides/navigating/) the parsed tree.
- [Building & editing](/xml_rs/guides/building/) documents from scratch.
